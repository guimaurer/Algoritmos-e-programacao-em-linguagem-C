/******************************************************************************

Teste de randon


*******************************************************************************/
#include <stdio.h>
#include <stdlib.h>

int main() {
  int qtd[5], tipo[5], aux, soma_qtd, scan_user;
  for (aux = 0; aux < soma_qtd; aux++) {
    if (tipo[aux] == 0) {
      qtd[aux] = scan_user;
      tipo[aux] = scan_user;
    }
  }
  soma_qtd++;
}
